class Solution {
    public String makeGood(String s) {
        StringBuffer buf = new StringBuffer();

        int i = 0;
        while (i < s.length()) {
            char ch = s.charAt(i);
            
        }
    }
}